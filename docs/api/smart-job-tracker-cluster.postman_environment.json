{
  "name": "Smart Job Tracker - Cluster",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:30080/api",
      "type": "default",
      "enabled": true
    },
    {
      "key": "appOrigin",
      "value": "http://localhost:30080",
      "type": "default",
      "enabled": true,
      "description": "The origin a browser loads the app from in the Compose and Kubernetes runtimes. Sent as Origin on 'Create job' so the run exercises the header a real browser attaches to every write."
    }
  ],
  "_postman_variable_scope": "environment"
}
