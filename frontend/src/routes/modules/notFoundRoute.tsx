import type { FC } from 'react';

import { NotFoundPage } from '../../pages/notFound/NotFoundPage';

/**
 * Renders the unmatched route page.
 *
 * @returns {JSX.Element} Not found route content.
 */
export const Component: FC = () => <NotFoundPage />;
